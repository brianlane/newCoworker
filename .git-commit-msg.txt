Bugbot round 2: honest one-per-person policy surface + post-claim daily-cap recount

The attendee duplicate guard staying on IS the policy for a public page
(one upcoming appointment per person keeps a single phone number from
strip-mining the calendar), but the visitor now gets an honest
"you already have an upcoming appointment" state (422 + localized copy)
instead of a generic failure. And concurrent submissions for DIFFERENT
slots on the same business-local day could each pass the cap check, so
submission recounts the day from the dedupe ledger after winning the
per-slot claim; the residual window is the ledger write itself, and the
cap is a courtesy limit, not a billing invariant.
