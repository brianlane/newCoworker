{
  "name": "nc-email-inbound",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker that forwards per-tenant AI mailbox mail to the app inbound webhook.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "postal-mime": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.125.0"
  },
  "overrides": {
    "sharp": "^0.35.3",
    "undici": "^7.29.0"
  }
}
